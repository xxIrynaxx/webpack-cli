{
  "name": "task1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:scripts": "SET NODE_OPTIONS=--openssl-legacy-provider && webpack -p",
    "watch:scripts": "SET NODE_OPTIONS=--openssl-legacy-provider && webpack -d --watch --devtool eval-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  }
}
